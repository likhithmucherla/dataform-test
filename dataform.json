{
  "defaultSchema": "${DEFAULT_SCHEMA_ENV_VAR}",
  "assertionSchema": "${ASSERTION_SCHEMA_ENV_VAR}",
  "warehouse": "${WAREHOUSE_ENV_VAR}",
  "defaultDatabase": "${DEFAULT_DATABASE_ENV_VAR}",
  "defaultLocation": "${DEFAULT_LOCATION_ENV_VAR}"
}
